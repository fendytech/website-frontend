import styled from 'styled-components';
import React from 'react';

const Search = styled.section`

`;



const TopNavigation = ({image}) => {

    return (
        
            
    );
};

export default TopNavigation;
